export const VERSION_APP = '4.0.0';

export const IS_DEV = process.env.NODE_ENV === 'development';
export const IS_PROD = process.env.NODE_ENV === 'production';

export const PUBLIC_URL = location.origin + '/public';
